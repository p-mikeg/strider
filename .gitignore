/target
fixtures/out
fixtures/vmlinux
/.worktrees
/.venv
__pycache__/
*.pyc
.pytest_cache/
*.so
*.dylib
*.dll

# Analyzer example output (cfg.dot/html, graph.dot/html, graph-opt.dot/html)
/*.dot
/*.html

# Stray rlib artifacts from out-of-tree test builds
/*.rlib

# Stray ELF artifacts from out-of-tree test builds
/rust_out

# Phase 4 Task 4.0 — auto-generated .pyi stubs from `cargo run -p
# strider-py --example stub_gen`.  Dev tool only; the wheel ships
# the hand-written `crates/strider-py/strider/pattern.pyi` instead.
/crates/strider-py/strider/_generated/
/uv.lock
