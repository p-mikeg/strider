[package]
name = "dot"
version = "0.1.0"
edition.workspace = true

[dependencies]
strider-ir.workspace = true

# Kept so external consumers depending on dot's transitive anyhow re-exports
# keep working through the shim.
anyhow.workspace = true

[lints]
workspace = true
