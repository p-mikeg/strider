[package]
name = "ir"
version = "0.1.0"
edition.workspace = true

[dependencies]
rsleigh.workspace = true
target.workspace = true
cranelift-bitset.workspace = true
cranelift-entity.workspace = true
smallvec.workspace = true
graphwalk.workspace = true
entity-utils.workspace = true
itertools.workspace = true
dot.workspace = true
anyhow.workspace = true
thiserror.workspace = true
ethnum.workspace = true

[dev-dependencies]
proptest = "1"
criterion = { version = "0.7", features = ["html_reports"] }

[[bench]]
name = "validate"
harness = false

[features]
# Exposes shared mock-IR test helpers (`make_empty_fn`, `reg_vn`, `sp_vn_x86`,
# `sp_vn_x86_64`, ...).  Test-only — not callable from production code.
test-utils = []

[lints]
workspace = true