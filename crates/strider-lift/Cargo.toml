[package]
name = "strider-lift"
version = "0.1.0"
edition.workspace = true

[dependencies]
strider-ir = { workspace = true }
dot.workspace = true
rsleigh.workspace = true
anyhow.workspace = true
# `target` source lives in the external crate; this module re-exports it
# (see src/target.rs).  See Phase 2 Task 2.3 cycle-break note.
target.workspace = true
# Absorbed from `cfg` (Phase 2 Task 2.3).
petgraph.workspace = true
rustc-hash = "2"

[dev-dependencies]
reader.workspace = true
object.workspace = true

[lints]
workspace = true
