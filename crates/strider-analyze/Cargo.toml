[package]
name = "strider-analyze"
version = "0.1.0"
edition.workspace = true

[dependencies]
strider-ir = { workspace = true }
strider-lift = { workspace = true }
# other deps added as modules absorb

[lints]
workspace = true
