[package]
name = "target"
version = "0.1.0"
edition.workspace = true

[dependencies]
strider-lift.workspace = true

# Kept so external consumers depending on target's transitive re-exports
# keep working through the shim.
rsleigh.workspace = true
anyhow.workspace = true
strider-ir.workspace = true

[lints]
workspace = true
