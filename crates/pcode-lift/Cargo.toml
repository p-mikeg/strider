[package]
name = "pcode-lift"
version = "0.1.0"
edition.workspace = true

[dependencies]
strider-lift.workspace = true

# Kept so external consumers depending on pcode-lift's transitive re-exports
# keep working through the shim.
rsleigh.workspace = true
ir.workspace = true
target.workspace = true
anyhow.workspace = true

[lints]
workspace = true
